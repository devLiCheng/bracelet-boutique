{
  "name": "bracelet-boutique",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch server/src/index.ts",
    "start": "bun run server/src/index.ts",
    "db:init": "bun run server/src/db/init.ts",
    "db:seed": "bun run server/src/db/seed.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
